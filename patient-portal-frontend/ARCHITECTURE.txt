PATIENT MANAGEMENT FRONTEND - EXPECTED ARCHITECTURE
===================================================

Status: Phases 1-3 and the initial Phase 4 specialized-operations slice complete;
        Phase 5 and invitation-based enrollment planned
Last reviewed and verified: 2026-09-11
Applies to: patient-portal-frontend

1. PURPOSE
----------

This directory should become a small, credible hospital and patient-management web
application. It must feel like an operational clinical product, not a marketing
site or a collection of demo cards. The first release should remain intentionally
minimal: support the highest-value workflows, make safety-critical state obvious,
and avoid presenting backend capabilities that are not actually implemented.

The frontend is a role-aware client for the API gateway at http://localhost:4004.
It must never connect directly to a service database or rely on browser-side role
checks as an authorization boundary. Backend authorization remains authoritative.

The current code is a Create React App/React 19/strict TypeScript application with
the Phase 1 foundation implemented. It has provider and route composition, shared
patient/staff shells, centralized role-aware navigation, an in-memory authenticated
session, a typed gateway boundary, accessible shared primitives and synthetic test
infrastructure. The minimal patient portal, core Phase 3 staff workflows and eight
Phase 4 specialized-operation routes are implemented against verified gateway
contracts. The architecture below records both the verified current implementation
and the planned end state.

Identity boundary update (2026-09-10): patient screens no longer derive or select
patientId from JWT claims. They load GET /api/portal/me, and the portal backend
resolves JWT sub through its validated ACTIVE portal_identity mapping. The returned
patientId is then used only in commands issued inside that already-authorized portal
session. The legacy patient-specific overview route is administrator-only.

2. PRODUCT SURFACES
-------------------

Implement one deployable web application with two shells sharing design tokens,
API infrastructure, authentication, error handling and accessibility primitives.

A. Patient portal shell

Primary users: patients and authorized proxies.

Minimum navigation:

  Overview
  Appointments
  Health records
  Bills and payments
  Profile and proxy access

Minimum patient workflows:

  - Sign in and sign out.
  - View an overview of appointment, record-request and payment activity.
  - Request an appointment and cancel an unresolved appointment request.
  - Request a copy of records and download a fulfilled text release.
  - Submit a payment intent with a unique Idempotency-Key.
  - View proxy grants and create/revoke a narrowly scoped proxy grant when the
    backend exposes enough data for a safe UI.
  - Show privacy-safe empty, loading, stale, denied and failure states.

B. Staff operations shell

Primary users: administrators, clinicians, nurses, registration staff, billing
staff, pharmacists, laboratory staff, privacy officers, auditors and analysts.

Minimum navigation is filtered by the authenticated role:

  Work queue              clinician/nurse/operational staff
  Patients                admin/clinician/registration staff
  Schedule                admin/clinician/registration staff
  Clinical chart          admin/clinician; selected safety actions for nurses
  Billing                 admin/billing staff
  Insurance               admin/billing staff
  Pharmacy and inventory  admin/pharmacist
  Notifications           permitted operational roles
  Compliance and audit    privacy officer/admin/auditor as applicable
  Reports                  admin/analyst

The initial staff release does not need every backend endpoint on day one. It does
need coherent patient search, patient context, task queues, appointment handling,
chart viewing, and clear links to the permitted specialized workflows.

3. INFORMATION ARCHITECTURE
---------------------------

Use URLs as durable application state. A user must be able to refresh a permitted
page without losing context. Suggested routes:

  /login
  /forbidden
  /patient/overview
  /patient/appointments
  /patient/records
  /patient/billing
  /patient/access

  /staff/work-queue
  /staff/patients
  /staff/patients/:patientId/summary
  /staff/patients/:patientId/demographics
  /staff/patients/:patientId/encounters
  /staff/patients/:patientId/medications
  /staff/patients/:patientId/labs
  /staff/patients/:patientId/billing
  /staff/schedule
  /staff/pharmacy
  /staff/inventory
  /staff/compliance
  /staff/audit
  /staff/reports

Patient context is a first-class state containing patient ID, MRN, name, date of
birth, lifecycle status and access state. Display a persistent patient banner on
every staff patient page. Never infer patient context from the last record viewed.
Changing patient context must clear patient-specific cached data before rendering
the next patient.

4. VISUAL AND INTERACTION DIRECTION
-----------------------------------

The application should resemble a real, restrained hospital system:

  - Light neutral surfaces, high-contrast text and one calm clinical accent color.
  - Dense but readable tables for queues, appointments and records.
  - A left navigation rail on desktop and a compact drawer on small screens.
  - A top bar containing facility/product name, environment label, user identity,
    current role, notifications entry point and sign-out action.
  - A patient banner below the top bar whenever staff is in patient context.
  - Status chips that combine words, color and icons; never communicate only with
    red/green color.
  - Forms with labels above controls, explicit required markers, inline validation,
    submission summaries and a visible cancel path.
  - Confirmation dialogs for cancellation, merge/unmerge, status changes,
    dispensing, note signing, record release, legal holds and other consequential
    mutations. The dialog must name the affected patient/resource.
  - No decorative gradients, oversized hero areas or excessive animations.
  - Skeletons for initial reads, unobtrusive progress for background refreshes,
    and explicit retry actions for recoverable failures.

Recommended desktop layout:

  +--------------------+-----------------------------------------------+
  | Product / facility | Environment | User / role | Sign out          |
  +--------------------+-----------------------------------------------+
  | Navigation         | Optional patient context banner               |
  |                    +-----------------------------------------------+
  |                    | Page title, actions and filters               |
  |                    +-----------------------------------------------+
  |                    | Alerts / validation summary                   |
  |                    +-----------------------------------------------+
  |                    | Main workflow content                         |
  +--------------------+-----------------------------------------------+

5. SOURCE ORGANIZATION
----------------------

Keep domain behavior close to its feature and cross-cutting infrastructure small.
Use this target structure:

  src/
    app/
      App.tsx                 route composition only
      AppProviders.tsx        query, auth, error and theme providers
      router.tsx              route tree and lazy feature boundaries
      routePolicy.ts          route-to-role presentation rules
    api/
      client.ts               fetch wrapper, headers and response decoding
      errors.ts               typed ApiError normalization
      generated/              generated OpenAPI types/client; never hand-edit
      queryKeys.ts            stable cache-key factories
    auth/
      AuthProvider.tsx
      session.ts              in-memory/session restoration policy
      tokenClaims.ts          defensive JWT claim parsing for presentation only
      RequireAuth.tsx
      RequireRole.tsx
    components/
      layout/                 AppShell, Nav, TopBar, PatientBanner
      feedback/               Alert, ErrorState, EmptyState, Skeleton
      forms/                  field wrappers and validation summary
      data/                   Table, Pagination, StatusBadge, DefinitionList
      safety/                 ConfirmAction, BreakGlassDialog
    features/
      patientOverview/
      patients/
      appointments/
      ehr/
      staffTasks/
      billing/
      insurance/
      pharmacy/
      notifications/
      compliance/
      audit/
      analytics/
    hooks/
    styles/
      tokens.css
      global.css
    test/
      handlers/               request mocks
      factories/              typed synthetic data only
      render.tsx              provider-aware test renderer
    types/
    index.tsx

  mockups/
    index.html                interactive route and screen reference
    styles.css                mockup-only clinical design tokens and layout
    app.js                    synthetic navigation, states and interactions
    README.md                 screen inventory and review instructions

The mockup suite is a design and workflow reference, not production source. Never
import mockup code or hard-coded records into `src/`. Production implementation
must use verified API types, shared React primitives and feature-owned state. When
an approved workflow change affects information hierarchy or interaction intent,
update the corresponding mockup and production documentation together.

Each feature should normally contain:

  api.ts                     query/mutation functions
  types.ts                   UI-only derived types, if needed
  routes.tsx                 feature route boundary
  components/                feature components
  pages/                     route-level screens
  validation.ts              form schema
  *.test.tsx                 behavior tests beside the code

Do not create global stores for server records. Server state belongs in the query
cache; local component state owns transient UI state; URL parameters own shareable
filters and selection; authentication owns only session identity and credentials.

6. RECOMMENDED CLIENT STACK
---------------------------

Retain React and TypeScript. The Phase 1 implementation uses:

  - React Router for nested, lazy, role-aware routing.
  - TanStack Query for server-state caching, invalidation and mutation state.
  - React Hook Form plus Zod for accessible forms and client validation.
  - Mock Service Worker for deterministic UI and integration tests.

OpenAPI generation with openapi-typescript or Orval remains planned for the feature
phases, when the deployed gateway contracts are stable enough to generate and diff.

Prefer CSS modules or a small token-based stylesheet over a large design framework.
If a component library is selected, document its accessibility status, bundle cost
and theming strategy before adoption. Do not mix multiple component systems.

TypeScript should run with strict mode when migration allows it. Avoid `any`; use
`unknown` at external boundaries and narrow it. Do not duplicate Java DTO shapes by
hand once reliable OpenAPI generation is available.

7. API CLIENT BOUNDARY
----------------------

All browser calls go through one API client configured with:

  REACT_APP_API_BASE_URL=http://localhost:4004

The client is responsible for:

  - Prefixing gateway paths.
  - Sending Accept: application/json, except FHIR reads which use
    application/fhir+json.
  - Sending Content-Type: application/json only when a JSON body exists.
  - Attaching Authorization: Bearer <token>.
  - Generating/propagating a correlation ID when supported.
  - Sending Idempotency-Key for patient creation, billing-account creation and
    portal payments. Keep the same key on a deliberate retry of the same action.
  - Parsing JSON, text downloads and empty 204 responses correctly.
  - Mapping 400, 401, 403, 404, 409, 410, 422/validation and 503 into a typed error.
  - Never logging authorization headers, request bodies containing PHI, downloaded
    records or raw backend errors to analytics/console in production.

Suggested error type:

  ApiError {
    status: number;
    code?: string;
    message: string;
    fieldErrors?: Record<string, string[]>;
    requestId?: string;
    retryable: boolean;
  }

Queries should use patient-bound keys such as ['patients', patientId, 'labs'].
Mutations invalidate the narrowest affected keys. Sensitive patient data should
default to a short stale time and must be cleared on logout, identity change,
patient-context change, 401, or detected revocation. Do not persist PHI query data
to localStorage, IndexedDB, service-worker caches or Redux persistence.

8. AUTHENTICATION AND AUTHORIZATION
-----------------------------------

Login uses POST /auth/login. Treat the JWT as a credential, not user profile data.
The frontend may decode claims to choose navigation, but every protected operation
must still tolerate a backend 403.

Preferred credential design for production is a secure, HttpOnly, SameSite cookie
issued by a backend-for-frontend. The current bearer-token API may require holding
the access token in memory. If session restoration is required before a BFF exists,
sessionStorage is less persistent than localStorage but remains exposed to XSS;
record and approve that tradeoff. Never place tokens in URLs.

Required session behavior:

  - Validate or reject a restored session before showing protected content.
  - On 401, clear all cached data and return to login with a neutral message.
  - On 403, keep the session but show an access-denied state.
  - Sign-out clears credentials, query caches, patient context and history state.
  - Inactivity timeout and absolute session lifetime must come from approved policy.
  - Disabling an account blocks new login immediately; revocation of an already-
    issued bearer token remains bounded by its short expiry until token-version,
    introspection or lifecycle-event enforcement is implemented.
  - Do not silently perform break-glass access. Require reason/evidence, show expiry,
    display a persistent emergency-access banner and expose review consequences.

Role-to-menu mapping is presentation logic only. Centralize it in routePolicy.ts so
buttons and routes use the same policy and tests can assert every role matrix.

9. DOMAIN FEATURE DESIGN
------------------------

Patient overview

  Use /api/portal/me for patient-facing activity; patient identity must be resolved
  by the server from JWT sub and never selected by the browser. For
  staff, compose a summary from permitted patient, appointment, EHR, billing and
  alert queries. Avoid large fan-out on every render; load the identity banner first
  and lazy-load tabs. Highlight allergies, active safety alerts and lifecycle status.

Patient registry

  Provide a compact searchable list, duplicate-candidate workflow, registration
  form, demographics edit and lifecycle actions. Merge screens must show source and
  survivor side by side and require a typed confirmation or equivalent deliberate
  action. Never use DELETE as a casual UI action; explain its actual backend effect.

Appointments

  Offer list/day views before building a complex calendar. Display timezone near
  all times. Booking validates future time and 5-480 minute duration. Cancellation
  shows the reason in the final review. Telemedicine join URLs are untrusted links:
  display the provider and require an explicit user click.

Clinical chart

  Separate encounters, history/allergies, diagnoses, prescriptions, labs, notes and
  vaccinations into predictable tabs. Signed notes are visually immutable. Show
  safety conflicts next to medication actions and never reduce a 409 safety response
  to a generic toast. FHIR Patient read is not a substitute for the internal chart.

Tasks

  Queue filters belong in the URL. Urgent/overdue work appears first. Claim, handoff,
  comment and complete actions update the visible history. Optimistic updates should
  be avoided for ownership/status changes because conflicts carry clinical meaning.

Billing and insurance

  Format money using the record currency; never use floating-point arithmetic for
  totals. Display invoice amount, paid amount and outstanding amount. Preserve
  idempotency references. Claims should expose submitted/adjudicated/paid state and
  explain conflicts without implying payer acceptance when none occurred.

Pharmacy and inventory

  Separate medication catalog, batches, movements, prescriptions awaiting review,
  dispensing and billing. Dispensing requires a recorded safety review and available
  unexpired stock. Consequential pharmacy actions should refetch rather than assume
  success from local state.

Compliance and audit

  Keep these screens separate. Compliance cases are workflows with evidence
  references and legal-hold state. Audit events are read-only evidence after append.
  Evidence references are identifiers, not places to paste patient details or URLs.

10. STATE, FORMS AND CONCURRENCY
--------------------------------

  - Read state: TanStack Query (or equivalent), scoped by identity and patient.
  - Form state: React Hook Form; reset only after confirmed success.
  - Navigation state: URL search/path parameters.
  - Modal state: local state unless it must survive navigation.
  - Session state: AuthProvider only.

Do not retry non-idempotent mutations automatically. GET requests may retry a small
number of times for network errors/503 with bounded exponential backoff. Do not
retry 400, 401, 403, 404, 409 or 410 automatically. A 409 often represents stale
workflow state, duplicate references, safety blocks or concurrency; refetch and ask
the user to review current state.

Prevent double submission at the UI, but rely on backend idempotency/locking for
correctness. Keep generated idempotency keys tied to the form attempt until success,
explicit reset or materially changed payload.

11. SECURITY AND PRIVACY REQUIREMENTS
-------------------------------------

  - Treat names, addresses, dates of birth, MRNs, diagnoses, medications, labs,
    appointments, invoices, proxy relationships and record downloads as sensitive.
  - No PHI in URLs except opaque identifiers already required by API paths.
  - No PHI in analytics, browser logs, error-report breadcrumbs or DOM attributes.
  - Disable client/service-worker caching for record downloads and FHIR responses.
  - Never render backend-provided HTML. Render all text as text; sanitize any future
    rich-text format with an approved policy.
  - Use rel="noopener noreferrer" for external consultation links.
  - Apply a restrictive Content Security Policy at hosting/gateway level.
  - Protect against clickjacking, MIME sniffing and referrer leakage with headers.
  - Keep third-party scripts off authenticated clinical pages unless approved.
  - Redact sensitive fields in test snapshots and use synthetic fixtures only.
  - Frontend checks never compensate for missing platform-wide patient/facility
    authorization. Do not expose legacy APIs externally until backend gates close.

12. ACCESSIBILITY AND RESPONSIVENESS
------------------------------------

Target WCAG 2.2 AA:

  - Full keyboard navigation and visible focus.
  - A skip-to-content link and semantic landmarks.
  - Correct heading hierarchy and associated form labels.
  - Tables with captions, headers and responsive alternatives.
  - Live regions for asynchronous status without excessive announcements.
  - Error summaries linked to invalid fields.
  - Minimum 4.5:1 text contrast and 3:1 component/focus contrast.
  - Touch targets suitable for tablets used during rounds.
  - Respect prefers-reduced-motion.
  - Dates, time zones, currency and language are explicit and locale-aware.

Desktop is primary for staff; tablet must support rounding and pharmacy workflows;
patient pages must work from 320px upward. Dense clinical tables may scroll within
clearly labelled regions rather than crushing columns.

13. TESTING STRATEGY
--------------------

Unit/component tests:

  - Field validation and payload transformation.
  - Loading, empty, denied, conflict, unavailable and success states.
  - Role-filtered navigation and route guards.
  - Patient-context cache isolation.
  - Currency/date formatting and status labels.

Integration tests with Mock Service Worker:

  - Login to overview.
  - Register/search/select a patient.
  - Request/cancel an appointment.
  - Create an encounter and sign a note.
  - Safety conflict blocks prescription/dispensing UI.
  - Submit an idempotent payment and handle a replay.
  - Place/release a legal hold through valid transitions.
  - 401 clears the session; 403 preserves it; 409 refetches current state.

End-to-end tests against a synthetic deployment:

  - One happy path per role family.
  - Cross-role and cross-patient denial.
  - Break-glass request/use/review behavior.
  - Download headers and no-store behavior.
  - Keyboard-only smoke test and automated accessibility checks.

14. OBSERVABILITY
-----------------

Record technical telemetry only: route template, operation name, duration, status
class, retry count, application version and server request/correlation ID. Never
record raw paths containing IDs, request/response bodies, form values or tokens.
Provide a user-visible request ID on unexpected errors so support can correlate it
with server logs. Use an environment badge so synthetic/staging sessions cannot be
mistaken for production.

15. DELIVERY PHASES
-------------------

Delivery proceeds through the following ordered phases. A later phase may be
explored in `mockups/`, but its production implementation does not begin until the
preceding phase exit gate is met. Each production route is implemented from the
approved interaction reference only after its fields, statuses, actions and roles
have been checked against the current backend contract.

PHASE 0 - INTERACTION BASELINE

Scope:

  1. Maintain the self-contained `mockups/` suite covering authentication, all
     patient portal routes, staff operations routes and shared system states.
  2. Review route hierarchy, role-filtered navigation, patient context, forms,
     tables, safety alerts and consequential confirmations with stakeholders.
  3. Record unsupported controls or fields as contract gaps instead of presenting
     them as working production capabilities.

Exit gate:

  - Every planned route has an identifiable mockup screen and primary user outcome.
  - Synthetic content, 320px-to-desktop responsiveness and keyboard-reachable
    representative interactions have been reviewed.
  - Mockup/API discrepancies are resolved or listed as explicit implementation gaps.

PHASE 1 - APPLICATION FOUNDATION

Status: COMPLETE (verified 2026-09-09)

Scope:

  1. Replace the hard-coded dashboard with AppProviders, route composition and the
     shared patient/staff application shells.
  2. Add design tokens and the shared accessible primitives defined by the source
     structure and the clinical design-system skill.
  3. Implement the typed gateway client, normalized ApiError handling, correlation
     IDs, response decoding and safe retry policy.
  4. Implement login, validation, logout, route guards, role navigation and session
     cache clearing without persistent PHI storage.
  5. Establish synthetic factories, MSW handlers, provider-aware rendering and CI
     checks for tests and production builds.

Exit gate:

  - Login, 401 session clearing, 403 session preservation and role-filtered routes
    pass automated tests.
  - The shared shell, feedback states and form primitives pass keyboard and basic
    automated accessibility checks at patient mobile and staff desktop widths.
  - Typecheck, unit tests and production build pass without production credentials
    or hard-coded patient records.

Verified implementation:

  - `AppProviders`, BrowserRouter route composition, shared patient/staff `AppShell`
    layouts and centralized `routePolicy` replace the original hard-coded dashboard.
    Every planned top-level patient and staff destination has a protected foundation
    route; workflow data remains deliberately deferred to the appropriate phase.
  - Semantic CSS tokens and shared layout, feedback, form, data and safety primitives
    are implemented. The shells include landmarks, a skip link, visible focus,
    responsive navigation and synthetic-environment/privacy indicators.
  - The typed gateway client adds bearer, correlation and idempotency headers,
    decodes JSON/text/204 responses, normalizes `ApiError` details and exposes bounded
    retry behavior only for transient reads. Mutations do not retry automatically.
  - Login uses React Hook Form and Zod validation. JWT claims are defensively decoded
    for presentation, restored credentials are validated, and route guards enforce
    authenticated and role-filtered presentation. Credentials remain in memory;
    logout and 401 clear the session and query cache, while 403 preserves both.
  - Synthetic factories, MSW authentication/error handlers and provider-aware test
    rendering are active. CI installs from the lockfile and runs typecheck, unit and
    accessibility tests, followed by the production build.
  - Exit-gate verification passed on 2026-09-09: strict typecheck, 35 automated tests
    across 3 suites, patient-mobile/staff-desktop axe checks, keyboard checks for
    responsive navigation, tabs and dialogs, and an optimized production build.

PHASE 2 - MINIMAL PATIENT PORTAL

Status: COMPLETE (verified 2026-09-09)

Scope:

  1. Implement Overview from `/api/portal/patients/{patientId}/overview`.
  2. Implement appointment requests and cancellation of unresolved requests.
  3. Implement record requests and no-store download of fulfilled text releases.
  4. Implement invoice/payment activity and idempotent payment submission.
  5. Implement profile presentation and only those proxy grant/revocation controls
     supported by safe identity selection and the verified backend contract.

Exit gate:

  - Login-to-overview, appointment request/cancel, record request/download and
    payment workflows pass integration tests with synthetic gateway responses.
  - Owner/proxy scope denials, duplicate payment replay, 409-not-ready download,
    empty, stale and 503 states are distinguishable and recoverable where allowed.
  - Patient pages work from 320px upward and expose no sensitive data to URLs,
    persistent storage, logs or analytics.

Verified implementation:

  - `/patient/overview` loads the authenticated portal overview with account state
    and appointment, record-request and payment summaries. Initial loading, empty,
    background-refresh, denied and unavailable states use shared feedback primitives.
  - `/patient/appointments` lists requests, validates and submits specialty/reason,
    and confirmation-gates cancellation only while a request is `REQUESTED`. A 409
    triggers authoritative overview invalidation instead of optimistic completion.
  - `/patient/records` validates a narrowly typed record request plus an explicit
    privacy acknowledgement. Fulfilled releases are downloaded as `records.txt`
    Blob files, never inserted into the DOM; 409-not-ready remains distinguishable.
  - `/patient/billing` displays decimal-formatted payment activity and validates
    invoice UUID and positive two-decimal amount input. One cryptographic
    Idempotency-Key remains tied to an unchanged payment attempt and is replaced
    after success or a materially changed payload.
  - `/patient/access` presents session and portal-account identity. Proxy create and
    revoke controls are deliberately withheld because the current gateway cannot
    list verified proxy identities or existing grants; presenting blind subject-ID
    entry would not meet the safe identity-selection requirement.
  - The current auth contract may provide `patient_id`; for PATIENT tokens without
    that claim, the UUID subject is used as the opaque development patient context
    and the portal service remains authoritative through its identity binding. A
    development-only migration binds the two synthetic PATIENT accounts. ADMIN
    sessions require an explicit patient_id claim before entering patient context.
  - Phase 2 has synthetic MSW coverage for login-to-overview data, empty activity,
    appointment request/cancel, record request/file download, payment retry with a
    stable key, owner denial, 409-not-ready and bounded 503 recovery. All five portal
    routes pass basic automated accessibility checks at 320px. The complete frontend
    gate passes strict typecheck, 46 tests across 4 suites and the production build.

Known contract boundaries:

  - The portal overview exposes payment submissions but no PATIENT-readable invoice
    list. Until that endpoint exists, the payment form accepts only the opaque invoice
    UUID issued on a statement and clearly identifies this limitation.
  - Proxy management remains presentation-only until verified identity discovery and
    grant-listing response contracts are available. This is an intentional safety
    boundary, not an unfinished visible control.

PHASE 3 - CORE STAFF AND CLINICAL WORKFLOWS - COMPLETE 2026-09-10

Verified implementation:

  - `/staff/work-queue` reads the authenticated role's queue with URL-owned role,
    priority and bounded-limit filters. It exposes claim, comment, self-handoff with
    a new due cycle, history and completion. Ownership/status mutations never update
    optimistically; 409 responses refresh the authoritative queue and remain visible
    as concurrency states. Administrators may select another permitted queue.
  - `/staff/patients` reads the current registry, applies URL-owned search and
    lifecycle filters, performs server duplicate checks before registration and
    creates patients with a cryptographic idempotency key retained for an unchanged
    deliberate retry. Search is intentionally client-filtered because the current
    patient list contract is unpaged and has no general search query.
  - `/staff/patients/:patientId/*` establishes patient context from a fresh patient
    lookup and displays a persistent identity banner. A patient-ID transition aborts
    outstanding reads and removes the previous patient's query subtree before the
    next record renders. Registration roles receive identity/demographic views only.
  - `/staff/schedule` provides URL-owned day/all-list modes, patient filtering,
    available doctor-schedule selection and booking with the backend's 5-480 minute
    validation. Booking conflicts invalidate the appointment list and require review.
  - Clinical routes provide summary, encounters/history/allergies, diagnoses and
    prescriptions, labs, notes and vaccinations. Eight patient-scoped reads compose
    one abortable chart query. Signed notes are explicitly locked. Alert
    acknowledgement, note signing and medication discontinuation name the affected
    patient/resource in confirmation dialogs and refetch after success or conflict.
  - Phase 3 uses feature-owned strict TypeScript contracts, normalizes provisional
    snake_case queue/alert maps at the API boundary and routes all requests through
    the configured gateway client. No staff or patient data is persistently cached.
  - Synthetic MSW coverage proves registry-to-chart context, queue claiming,
    appointment 409 handling, signed-note immutability, alert confirmation and a
    basic accessibility scan. The full frontend gate passes strict typecheck,
    45 tests across 5 suites and an optimized production build.

Known contract boundaries:

  - The backend has no staff-directory lookup. Handoff therefore supports only the
    authenticated actor and a new due cycle; the UI does not ask users to type an
    opaque assignee UUID. Reassignment to another staff member requires a verified
    staff-selection contract.
  - The patient service does not authorize NURSE and exposes no minimum-necessary
    nurse identity-banner endpoint. The nurse clinical landing page explains this
    boundary instead of opening an unverified patient chart. Assigned queue and
    safety APIs remain role-gated by their services.
  - General patient search is not a server contract and `GET /api/patients` remains
    unpaginated. The current client filter is suitable only for the bounded initial
    deployment; a paginated search DTO is required before representative scale.
  - The appointment contract exposes doctor UUIDs and schedules but no staff display
    directory or explicit slot resource. The UI presents the verified schedule date,
    time and location and relies on booking conflict enforcement for concurrency.

PHASE 4 - SPECIALIZED OPERATIONS

Implementation status: initial operational slice delivered on 2026-09-11.

Scope:

  1. Billing and insurance: accounts, invoices, postings, policy coverage, claims,
     adjudication, remittance and reconciliation.
  2. Pharmacy and inventory: catalogue, batches, movements, safety review,
     dispensing, medication charge and billing.
  3. Notifications: permitted operational messages and delivery state.
  4. Privacy and compliance: consent, emergency-access review, cases, transitions
     and legal holds.
  5. Audit and analytics: read-only evidence, population trends, cohorts and
     regulatory reports.

Exit gate:

  - Each specialty route passes its role matrix, state-transition, idempotency or
    concurrency tests as applicable.
  - Pharmacy safety, claim/payment state, legal holds and emergency access never use
    optimistic completion and always refetch authoritative results.
  - Audit/analytics views prevent raw identifiers, PHI and small sensitive cells
    from leaking through telemetry or exports.

Verified implementation:

  - `/staff/billing` lists accounts, creates idempotent patient accounts, loads
    patient invoices, creates reference-idempotent invoices and posts payments.
    Payment conflicts refetch authoritative account/invoice state.
  - `/staff/insurance` lists coverage evidence and claims. Submitted-claim approval
    or denial and approved-claim reconciliation require confirmation; stale or
    incomplete remittance state is never shown as locally complete.
  - `/staff/pharmacy` records the three required safety checks and separates that
    approval from confirmed dispensing. Dispensing refetches medication stock and
    charge state after success or conflict.
  - `/staff/inventory` highlights reorder exposure, updates supply balances and
    receives future-dated medication batches using stable receipt references.
  - `/staff/notifications` queues minimum-necessary operational messages and shows
    delivery status/attempts without equating HTTP 202 acceptance with delivery.
  - `/staff/compliance` is limited to PRIVACY_OFFICER because the current backend
    case and review contracts do not authorize ADMIN. It creates cases, filters
    overdue work, confirms state transitions and case-level holds, and performs
    independent emergency-access review/revocation with evidence references.
  - `/staff/audit` provides URL-owned patient/actor filters over append-only evidence
    and intentionally exposes no browser mutation or export action.
  - `/staff/reports` lists regulatory reports, population trends and disease cohorts,
    accepts regulatory report requests and suppresses cohort counts below five. It
    exposes no patient drill-down or browser export.
  - Every route uses the shared authenticated gateway client, query cache, loading,
    empty, denial, conflict and retry states. HTTP 502 dependency failures are now
    treated as transient alongside 503. No specialized-operation response is
    persisted to browser storage.
  - Synthetic MSW tests cover all eight routes across their specialist roles,
    confirmation-before-dispensing, 409 behavior, compliance transitions,
    append-only audit presentation, small-cell suppression and basic accessibility.

Known Phase 4 boundaries:

  - The current slice does not expose every creation endpoint. Policy enrollment,
    claim/remittance entry, medication catalogue creation, inbound prescription
    creation, pharmacy invoice initiation, consent creation and trend/cohort authoring
    remain backend-supported workflows for a later UI increment.
  - Billing and pharmacy roles cannot query the patient registry, so these screens
    accept UUIDs from the operator's verified upstream work item. A minimum-necessary
    patient lookup/directory contract is required before replacing those identifiers
    with a safe patient selector.
  - Pharmacy exposes no list-prescriptions backend endpoint. The controlled workflow
    therefore starts from a prescription UUID supplied by the pharmacy work queue.
  - Audit hashes are abbreviated for visual verification. Independent chain
    validation and evidence export remain controlled operational processes outside
    the browser.

PHASE 5 - INTEGRATION, HARDENING AND RELEASE

Scope:

  1. Run one synthetic end-to-end journey per role family plus cross-role and
     cross-patient denial tests.
  2. Complete accessibility review, keyboard-only journeys, responsive verification
     and supported browser/device checks.
  3. Verify content security policy, cache/download headers, telemetry redaction,
     dependency risk, source-map policy and performance budgets.
  4. Diff generated API contracts against the deployed backend revision, update
     fixtures and resolve or document drift.
  5. Obtain the named privacy, security and release approvals required by policy.

Exit gate:

  - All criteria in section 16 pass for every release-scope feature.
  - The synthetic integrated deployment passes critical journeys without unresolved
    severity-one safety, privacy, security or accessibility defects.
  - Engineering may label the result `release candidate`; only the named approving
    authority may label it `production approved`.

16. DEFINITION OF DONE FOR A FRONTEND FEATURE
---------------------------------------------

A feature is done only when:

  - It uses the gateway API and generated/verified contract types.
  - Its permitted roles and patient-context behavior are documented and tested.
  - Loading, empty, success, validation, 401, 403, 404, 409 and 503 behavior is
    handled where relevant.
  - Mutations have deliberate confirmation and idempotency behavior where required.
  - No PHI/token leaks occur through logs, storage, URLs or telemetry.
  - Keyboard and screen-reader basics pass; responsive layouts are verified.
  - Tests use synthetic data and cover the primary user outcome.
  - The API.txt contract and this architecture remain accurate after the change.
