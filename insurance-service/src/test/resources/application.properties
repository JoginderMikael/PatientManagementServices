spring.datasource.url=jdbc:h2:mem:insurance-service;MODE=PostgreSQL;DATABASE_TO_LOWER=TRUE;DB_CLOSE_DELAY=-1
spring.datasource.username=sa
spring.datasource.password=
jwt.secret=${JWT_SECRET}
jwt.issuer=patient-management-auth
jwt.audience=patient-management-api
app.audit.enabled=false
spring.application.name=insurance-service
logging.level.root=WARN
